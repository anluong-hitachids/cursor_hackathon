TASK:
Convert the Java ETL transformation logic into Python.

CONSTRAINTS:
- Implement a pure function: transform(lines: list[str])
- No file I/O
- Preserve all validation rules

OUTPUT:
Only the Python function code.
