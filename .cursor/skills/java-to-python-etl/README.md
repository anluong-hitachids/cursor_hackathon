# java-to-python-etl

Reusable Cursor skill for **controlled code migration and refactoring**.

**Primary workflow:** Java → Python ETL — phased prompts from context through review ([SKILL.md](SKILL.md)).

**Planned extensions:** legacy cleanup; standalone test-generation workflows (same phased discipline).

Enable by adding this folder under `.cursor/skills/` (or symlink/copy to `~/.cursor/skills/` for personal use).
